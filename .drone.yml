pipeline:
  lint:
    image: node:${NODE_VERSION}
    commands:
      - npm i
      - npm run lint
      - npm run test

matrix:
  NODE_VERSION:
    - "10-alpine"
    - "9-alpine"
    - "8.11-alpine"
    - "alpine"